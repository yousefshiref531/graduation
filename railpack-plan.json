{
  "build": {
    "builder": "NIXPACKS",
    "steps": [
      "pip install -r requirements.txt"
    ]
  },
  "deploy": {
    "startCommand": "uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
